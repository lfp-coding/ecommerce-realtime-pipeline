# ==============================================================================
# E-COMMERCE REAL-TIME ANALYTICS PIPELINE - ENVIRONMENT TEMPLATE
# Copy this file to ".env" and adjust values for your local setup.
# No SASL/auth is used; all Kafka listeners are PLAINTEXT in docker-compose.==============================================================================

# ------------------------------------------------------------------------------
# Application
# ------------------------------------------------------------------------------
APP_ENV=local                 # Environment: local | dev | prod
SERVICE_NAME=ecommerce-pipeline
LOG_LEVEL=INFO                # DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_FORMAT=console            # console | json
LOG_TO_FILE=false             # true enables file logging to logs/app.log
LOG_FILE_MAX_BYTES=10485760   # 10MB rotation size
LOG_FILE_BACKUP_COUNT=5       # Number of rotated files to keep
BATCH_SIZE=50                 # Generic batch size for consumers/workers

# ------------------------------------------------------------------------------
# Kafka - Cluster (KRaft) Configuration
# ------------------------------------------------------------------------------
# Generate a unique cluster ID once for your local cluster:
# docker run --rm confluentinc/cp-kafka:7.7.1 kafka-storage random-uuid
KAFKA_CLUSTER_ID=REPLACE_WITH_BASE64_UUID

# ------------------------------------------------------------------------------
# Kafka - Client (Producers/Consumers) Configuration
# ------------------------------------------------------------------------------
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_CLIENT_ID=ecommerce-app

# Topics
KAFKA_ORDER_TOPIC=orders
KAFKA_CUSTOMER_TOPIC=customers
KAFKA_PRODUCT_TOPIC=products
KAFKA_EVENT_TOPIC=events

# ------------------------------------------------------------------------------
# PostgreSQL
# ------------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecommerce_analytics
DB_USER=admin
DB_PASSWORD=your-strong-db-password
DB_TEST_NAME=ecommerce_analytics_test

# ------------------------------------------------------------------------------
# Dashboard (Streamlit)
# ------------------------------------------------------------------------------
DASHBOARD_PORT=8501

# ------------------------------------------------------------------------------
# Monitoring (placeholders for future activation)
# ------------------------------------------------------------------------------
METRICS_ENABLED=false
HEALTHCHECK_INTERVAL_SECONDS=30

# ------------------------------------------------------------------------------
# pgAdmin (for local database management)
# ------------------------------------------------------------------------------
PGADMIN_EMAIL=admin@ecommerce.com
PGADMIN_PASSWORD=your-strong-pgadmin-password
